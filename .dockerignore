# Build context for the backend image (examples/agent_service/Dockerfile).
# Keep: pyproject.toml, README.md, src/, examples/agent_service/ (sans workspaces).

.git
.github
.gemini
docs
tests
assets
scripts
.pre-commit-config.yaml

# The frontend has its own image / build context.
examples/web_ui

# Runtime data generated by the agent service.
examples/agent_service/workspaces

# Python / build artifacts
**/__pycache__
**/*.pyc
**/*.pyo
**/*.egg-info
.venv
venv
build
dist
.pytest_cache
.mypy_cache
.ruff_cache
